config:
  target: 'http://localhost:5000'
  phases:
    - duration: 60  # Duration of the test in seconds
      arrivalRate: 50  # Number of requests per second
scenarios:
  - flow:
      - get:
          url: '/'